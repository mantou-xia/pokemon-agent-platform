# Backlog

## Purpose

This file tracks shared, not-yet-scheduled work that is broader than a single user task list.

## Current Backlog

- refine `/space` document set as the project grows
- define initial `services/api` scope and contracts
- define structured Pokemon data schema boundaries
- define first plugin tool set and ownership
- define initial knowledge ingestion pipeline for crawler output
