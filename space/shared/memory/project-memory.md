# Project Memory

## Purpose

This file holds shared long-term context that is useful across users, tasks, and agents.

## Current Shared Memory

- This repository is both a Pokemon Agent product and an AI-native software engineering workspace.
- `space/` is the persistent agent workspace, not a passive document folder.
- `services/api` should remain the business control plane between product clients and Dify.
- RAG is for explanation and retrieval; deterministic tools and structured data are for exact calculation.

## Promotion Rule

Only add information here when it is stable, cross-task useful, and not merely personal preference.
