# Personal Memory

## Purpose

This file stores user-specific long-term working preferences and recurring constraints for this repository.

## Active Preferences

- Start from `space/README.md`, then load only the context needed for the task.
- Plan before coding.
- Treat `/space` as an operational runtime workspace, not a passive notes folder.
- Keep README thin; push detailed rules into specialized files.
- Use `todolist/` as the canonical task-state directory.

## Update Rule

Add only durable preferences or constraints that should influence future work in this repository.
